Name:           ros-indigo-message-store-map-switcher
Version:        0.0.4
Release:        0%{?dist}
Summary:        ROS message_store_map_switcher package

Group:          Development/Libraries
License:        MIT
Source0:        %{name}-%{version}.tar.gz

Requires:       SDL_image-devel
Requires:       ros-indigo-map-server
Requires:       ros-indigo-mongodb-store
Requires:       ros-indigo-nav-msgs
Requires:       yaml-cpp-devel
BuildRequires:  SDL_image-devel
BuildRequires:  ros-indigo-catkin
BuildRequires:  ros-indigo-map-server
BuildRequires:  ros-indigo-message-generation
BuildRequires:  ros-indigo-mongodb-store
BuildRequires:  ros-indigo-nav-msgs
BuildRequires:  yaml-cpp-devel

%description
A package for manipulating and serving maps from the mongodb_store.

%prep
%setup -q

%build
# In case we're installing to a non-standard location, look for a setup.sh
# in the install tree that was dropped by catkin, and source it.  It will
# set things like CMAKE_PREFIX_PATH, PKG_CONFIG_PATH, and PYTHONPATH.
if [ -f "/opt/ros/indigo/setup.sh" ]; then . "/opt/ros/indigo/setup.sh"; fi
mkdir -p build && cd build
%cmake .. \
        -UINCLUDE_INSTALL_DIR \
        -ULIB_INSTALL_DIR \
        -USYSCONF_INSTALL_DIR \
        -USHARE_INSTALL_PREFIX \
        -ULIB_SUFFIX \
        -DCMAKE_INSTALL_PREFIX="/opt/ros/indigo" \
        -DCMAKE_PREFIX_PATH="/opt/ros/indigo" \
        -DSETUPTOOLS_DEB_LAYOUT=OFF \
        -DCATKIN_BUILD_BINARY_PACKAGE="1" \

make %{?_smp_mflags}

%install
# In case we're installing to a non-standard location, look for a setup.sh
# in the install tree that was dropped by catkin, and source it.  It will
# set things like CMAKE_PREFIX_PATH, PKG_CONFIG_PATH, and PYTHONPATH.
if [ -f "/opt/ros/indigo/setup.sh" ]; then . "/opt/ros/indigo/setup.sh"; fi
cd build
make %{?_smp_mflags} install DESTDIR=%{buildroot}

%files
/opt/ros/indigo

%changelog
* Thu Oct 30 2014 Nick Hawes <n.a.hawes@cs.bham.ac.uk> - 0.0.4-0
- Autogenerated by Bloom

